		<div class="hero-unit customHero">
			<h1><i class="icon-credit-card"></i> Confirm Your Order</h1>
		</div>
	</div>
</div>

<table class="table">
	<tr>
		<th>Title</th>
		<th>Artist</th>
		<th>Album</th>
		<th>Genre</th>
		<th>Length</th>
		<th>Price</th>
	</tr>
	<% @basket.line_items.each do |item| %>
		<tr>
			<td><%= item.track.title %></td>
			<td><%= item.track.artist.name %></td>
			<td><%= item.track.album.name %></td>
			<td><%= item.track.genre.name %></td>
			<td><%= (Time.mktime(0) + item.track.length).strftime("%M:%S.%L") %></td>
			<td><%= number_to_currency(item.track.price, :unit => "&pound;") %></td>
		</tr>
	<%end%>
</table>

<%= link_to "Confirm", :controller => 'paypal_express', :action => 'purchase' %>